project TypedefCollections {

    typedef PositiveInteger Integer with (PositiveInteger > 0);   
    typedef SpringInteger PositiveInteger with (SpringInteger < 15);   
    
    setOf(SpringInteger) springValues;
    springValues = {2, -11, 13}; 
    
}
