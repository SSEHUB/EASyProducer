project attributeAssignment2 {

    version v0;
    enum States {One, Two};
    attribute States state to attributeAssignment2;
    assign (state = States.One) to {
        Integer a;
    }
    assign (state = States.Two) to {
        Integer b;
    }
}