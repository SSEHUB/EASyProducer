project testEnumerations2 {

    version v0;
    enum colors2 {red = 1, green = 5, blue = 10};
    colors2 minVal = min(colors2.red, colors2.blue);
    //* colors2 maxVal = max(colors2.red, colors2.green);
    colors2 maxVal = colors2.red.max(colors2.green);
}