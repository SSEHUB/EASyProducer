project integerFunctions {

    Integer var = 0;
    String s;
    Real r;
    var == 0;
    //* var == 0;
    ==(var, 0);
    //* var == 0;
    var .==(0);
    var <> 0;
    //* var <> 0;
    <>(var, 0);
    //* var <> 0;
    var .<>(0);
    //* var <> 0;
    var != 0;
    //* var != 0;
    !=(var, 0);
    //* var != 0;
    var .!=(0);
    isDefined(var);
    //* isDefined(var);
    var.isDefined();
    isKindOf(var, Integer);
    //* isKindOf(var, Integer);
    var.isKindOf(Integer);
    isTypeOf(var, Integer);
    //* isTypeOf(var, Integer);
    var.isTypeOf(Integer);
    s = locale(var);
    //* s = locale(var);
    s = var.locale();
    isKindOf(var, typeOf(var));
    //* isKindOf(var, typeOf(var));
    var.isKindOf(var.typeOf());
    s = asType(var, String);
    //* s = asType(var, String);
    s = var.asType(String);
    var < 1;
    //* var < 1;
    <(var, 1);
    //* var < 1;
    var.<(1);
    var > 1;
    //* var > 1;
    >(var, 1);
    //* var > 1;
    var.>(1);
    var <= 1;
    //* var <= 1;
    <=(var, 1);
    //* var <= 1;
    var.<=(1);
    var >= 1;
    //* var >= 1;
    >=(var, 1);
    //* var >= 1;
    var.>=(1);
    var = var + 1;
    //* var = var + 1;
    var = +(var, 1);
    //* var = var + 1;
    var = var.+(1);
    var = - var;
    //* var = - var;
    var = var.-();
    var = var - 1;
    //* var = var - 1;
    var = var.-(1);
    var = var * 1;
    //* var = var * 1;
    var = *(var, 1);
    //* var = var * 1;
    var = var.*(1);
    r = var / 1;
    //* r = var / 1;
    r = /(var, 1);
    //* r = var / 1;
    r = var./(1);
    var = abs(var);
    //* var = abs(var);
    var = var.abs();
    var = div(var, 1);
    //* var = div(var, 1);
    var = var.div(1);
    var = min(var, 1);
    //* var = min(var, 1);
    var = var.min(1);
    var = max(var, 1);
    //* var = max(var, 1);
    var = var.max(1);
    var = mod(var, 1);
    //* var = mod(var, 1);
    var = var.mod(1);
    s = toString(var);
    //* s = toString(var);
    s = var.toString();

}