project enumFunctions {

    enum myEnum {one, two, three};
    enum myOrderedEnum {one = 1, two = 2, three = 3};
    myEnum var = myEnum.one;
    myOrderedEnum varO = myOrderedEnum.one;
    String s;
    Integer i;
    var == myEnum.two;
    var <> myEnum.three;
    //* var <> myEnum.three;
    <>(var, myEnum.three);
    //* var <> myEnum.three;
    var .<>(myEnum.three);
    //* var <> myEnum.three;
    var != myEnum.three;
    //* var != myEnum.three;
    !=(var, myEnum.three);
    //* var != myEnum.three;
    var .!=(myEnum.three);
    isDefined(var);
    //* isDefined(var);
    var.isDefined();
    isKindOf(var, myEnum);
    //* isKindOf(var, myEnum);
    var.isKindOf(myEnum);
    isTypeOf(var, myEnum);
    //* isTypeOf(var, myEnum);
    var.isTypeOf(myEnum);
    s = locale(var);
    //* s = locale(var);
    s = var.locale();
    isKindOf(var, typeOf(var));
    //* isKindOf(var, typeOf(var));
    var.isKindOf(var.typeOf());
    s = asType(var, String);
    //* s = asType(var, String);
    s = var.asType(String);
    i = ordinal(var);
    //* i = ordinal(var);
    i = var.ordinal();
    i = ordinal(myEnum.one);
    varO == myOrderedEnum.two;
    varO <> myOrderedEnum.three;
    isDefined(varO);
    //* isDefined(varO);
    varO.isDefined();
    isKindOf(varO, myOrderedEnum);
    //* isKindOf(varO, myOrderedEnum);
    varO.isKindOf(myOrderedEnum);
    isTypeOf(varO, myOrderedEnum);
    //* isTypeOf(varO, myOrderedEnum);
    varO.isTypeOf(myOrderedEnum);
    s = locale(varO);
    //* s = locale(varO);
    s = varO.locale();
    isKindOf(varO, typeOf(varO));
    //* isKindOf(varO, typeOf(varO));
    varO.isKindOf(varO.typeOf());
    s = asType(varO, String);
    //* s = asType(varO, String);
    s = varO.asType(String);
    i = ordinal(varO);
    //* i = ordinal(varO);
    i = varO.ordinal();
    i = ordinal(myOrderedEnum.one);
    varO < myOrderedEnum.one;
    //* varO < myOrderedEnum.one;
    <(varO, myOrderedEnum.one);
    //* varO < myOrderedEnum.one;
    varO.<(myOrderedEnum.one);
    varO > myOrderedEnum.one;
    //* varO > myOrderedEnum.one;
    >(varO, myOrderedEnum.one);
    //* varO > myOrderedEnum.one;
    varO.>(myOrderedEnum.one);
    varO <= myOrderedEnum.one;
    //* varO <= myOrderedEnum.one;
    <=(varO, myOrderedEnum.one);
    //* varO <= myOrderedEnum.one;
    varO.<=(myOrderedEnum.one);
    varO >= myOrderedEnum.one;
    //* varO >= myOrderedEnum.one;
    >=(varO, myOrderedEnum.one);
    //* varO >= myOrderedEnum.one;
    varO.>=(myOrderedEnum.one);
    varO = min(varO, varO);
    //* varO = min(varO, varO);
    varO = varO.min(varO);
    varO = max(varO, varO);
    //* varO = max(varO, varO);
    varO = varO.max(varO);
}