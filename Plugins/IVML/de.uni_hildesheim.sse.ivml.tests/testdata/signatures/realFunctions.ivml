project realFunctions {

    Real var = 0.0;
    String s;
    var == 0.0;
    //* var == 0.0;
    ==(var, 0.0);
    //* var == 0.0;
    var .==(0.0);
    var <> 0.0;
    //* var <> 0.0;
    <>(var, 0.0);
    //* var <> 0.0;
    var .<>(0.0);
    //* var <> 0.0;
    var != 0.0;
    //* var != 0.0;
    !=(var, 0.0);
    //* var != 0.0;
    var .!=(0.0);
    isDefined(var);
    //* isDefined(var);
    var.isDefined();
    isKindOf(var, Real);
    //* isKindOf(var, Real);
    var.isKindOf(Real);
    isTypeOf(var, Real);
    //* isTypeOf(var, Real);
    var.isTypeOf(Real);
    s = locale(var);
    //* s = locale(var);
    s = var.locale();
    isKindOf(var, typeOf(var));
    //* isKindOf(var, typeOf(var));
    var.isKindOf(var.typeOf());
    s = asType(var, String);
    //* s = asType(var, String);
    s = var.asType(String);
    var < 1.0;
    //* var < 1.0;
    <(var, 1.0);
    //* var < 1.0;
    var.<(1.0);
    var > 1.0;
    //* var > 1.0;
    >(var, 1.0);
    //* var > 1.0;
    var.>(1.0);
    var <= 1.0;
    //* var <= 1.0;
    <=(var, 1.0);
    //* var <= 1.0;
    var.<=(1.0);
    var >= 1.0;
    //* var >= 1.0;
    >=(var, 1.0);
    //* var >= 1.0;
    var.>=(1.0);
    var = var + 1.0;
    //* var = var + 1.0;
    var = +(var, 1.0);
    //* var = var + 1.0;
    var = var.+(1.0);
    var = - var;
    //* var = - var;
    var = var.-();
    var = var - 1.0;
    //* var = var - 1.0;
    var = var.-(1.0);
    var = var * 1.0;
    //* var = var * 1.0;
    var = *(var, 1.0);
    //* var = var * 1.0;
    var = var.*(1.0);
    var = var / 1.0;
    //* var = var / 1.0;
    var = /(var, 1.0);
    //* var = var / 1.0;
    var = var./(1.0);
    var = abs(var);
    //* var = abs(var);
    var = var.abs();
    var = floor(var);
    //* var = floor(var);
    var = var.floor();
    var = round(var);
    //* var = round(var);
    var = var.round();
    var = min(var, 1.0);
    //* var = min(var, 1.0);
    var = var.min(1.0);
    var = max(var, 1.0);
    //* var = max(var, 1.0);
    var = var.max(1.0);
    s = toString(var);
    //* s = toString(var);
    s = var.toString();

}