project ivmlSpec_attributes {

    enum BindingTimes {configuration = 0, compile = 1, runtime = 2};
    compound ContentSharing {
    }

    ContentSharing contentSharing;
    annotate BindingTimes binding = BindingTimes.compile to contentSharing;
}