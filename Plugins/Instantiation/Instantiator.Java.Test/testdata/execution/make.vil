vilScript vtl (Project source, Project target, Configuration config) {

    main(Project source, Project target, Configuration config) = : {
        String pfad = "/antTestFiles/makeFile/";
        String buildTarget = "make";
        String arguments = "Product=JNI";
        make(source.getPath(), pfad, buildTarget, n=arguments);
    }

}