import javaLib;

@indent(indentation = 4, additional = 0)
@format(profile="java", profileArg_javadocIndent="* ", lineLength = 120, profileArg_eltSorting="CONS_ALPHA")
template JavaCode5Tpl(Configuration config, JavaCodeArtifact target) {

    def main(Configuration config, JavaCodeArtifact target) {
        JavaCodeClass mainCls = target.addClass("Data",
            'Data class.
            Generated by: EASy-Producer.');
   
        mainCls.addConstructor();
    
        JavaCodeAttribute a = mainCls.addAttribute("int", "amount");
        mainCls.addSetter(a);
        mainCls.addGetter(a);

        a = mainCls.addAttribute("double", "price");
        mainCls.addSetter(a);
        mainCls.addGetter(a);

    }
    
}
