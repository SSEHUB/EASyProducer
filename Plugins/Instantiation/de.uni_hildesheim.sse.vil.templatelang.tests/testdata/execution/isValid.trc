-> main(Configuration, FileArtifact) with (<config>, test.texts)
    cfg <- <config>
    -> isValid(IvmlElement) with (adviceTestVM1::intVal) = true
    -> isValid(IvmlElement) with (adviceTestVM1::c1) = true