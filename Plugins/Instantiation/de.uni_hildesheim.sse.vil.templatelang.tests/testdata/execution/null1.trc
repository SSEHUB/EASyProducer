-> main(Configuration, FileArtifact) with (<config>, test.texts)
    cfg <- <config>
    value <- null
    -> !=(Integer, Integer) with (null, 1) = null
    visit else-branch
    -> !=(Integer, Integer) with (null, null) = false
    visit else-branch
    -> ==(Integer, Integer) with (null, null) = true
    visit if-branch