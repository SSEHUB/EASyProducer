template loop1(Configuration config, Artifact target) {

    def main(Configuration config, Artifact target) {
        String name;
        for (DecisionVariable v : config.variables()) {
            name = v.name();
        }
    }

}