vilScript nestedTypes (Project source, Project target, Configuration config) {

    mapOf(Integer, Integer) map1 = {};
    mapOf(Integer, mapOf(Integer, Integer)) map2 = {};
    setOf(mapOf(Integer, Integer)) set1 = {};

    main() = : {
        test1({});
        test2({});
    }

    test1(mapOf(Integer, Integer) m) = : {
    }

    test2(mapOf(Integer, mapOf(Integer, Integer)) m) = : {
    }

}