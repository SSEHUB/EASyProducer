vilScript generics () {

    Integer test = 5;

    productionGenericCopy(FileArtifact x, FileArtifact y) = y : x {
        x.copy(y);
    }
	
	protected produceContent() = {
	    new FileArtifact();
	}

}