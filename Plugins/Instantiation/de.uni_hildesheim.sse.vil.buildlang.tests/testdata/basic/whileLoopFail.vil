vilScript whileLoop (Project source, Configuration config) {

    main(Project source, Configuration config) = : {
        Integer i = 0;
        while("test") {
            i = i + 1;
        };
    }

}