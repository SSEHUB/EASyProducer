import basePL;

vilScript extendedPL (Project source, Project target, Configuration config) {

    main(Project source, Project target, Configuration config) = : {
        basePL::main(source, target, config);
    }

}