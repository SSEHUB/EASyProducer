vilScript generics () {

    main() = : {
        StringCreator sc = new StringCreator();
        mapOf(String, Real) weights = {};
        mapOf(String, mapOf(String, Real)) results;
        results = sc.mapStrings("", weights);
    }

}