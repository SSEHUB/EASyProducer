-> main(Project, Configuration, Project) with (<project>, <config>, <project>)
    -> t(Configuration) with (<config>)
        dp <- <config>
        a <- dispatch2::alg
        -> first(Sequence) with ({dispatch2::Algorithm::avail [0]}) = dispatch2::Algorithm::avail [0]
        a.act <- dispatch2::Algorithm::avail [0]
        -> process(MPCCNode) with (dispatch2::Algorithm::act)
        Reasoner execution: true
    -> enact(Project, Configuration, Project) with (<project>, <config>, <project>)