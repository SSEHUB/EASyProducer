-> main(Project, Configuration, Project) with (<project>, <config>, <project>)
    -> tPass()
        params <- {}
        -> first(Sequence) with ({}) = null
        param <- null
        -> setParameter(Parameter, Any) with (null, 15)
        done <- false
        -> ==(Boolean, Boolean) with (false, null) = false
        visit else-branch
        -> !(Boolean) with (false) = true
        visit if-branch
        explicit fail message "not done"