This is a testcase project. Some settings happen automatically. For manual demonstration:
  - delete CallCurlTemplate (if it still exists)
  - copy the build.properties file that is appropriate for your system and rename it to 
    build.properties. Done automatically by the test.