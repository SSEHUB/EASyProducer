project InfrastructureCfg {
	
	import Infrastructure;
	import PipelinesCfg;
	
	//refTo(Pipeline) pipeline;
	Pipeline pipeline;
	
	pipeline = Pipeline {
		name = "test",
		sources = {refBy(src)}
	};
	
	freeze {
	    pipeline;
	}

}