project SimpleMesh3AllTypes {

    import SimpleMesh3AllTypesPart*; 
    
    // unusual for managed model structure but required for reuse in testing/evaluation environment
    
    annotate BindingTime bindingTime = BindingTime::compile to .;
    
    freeze {
        .;
    }
}