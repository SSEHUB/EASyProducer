package iip.serializers;

import java.io.IOException;
import de.iip_ecosphere.platform.transport.serialization.*;
import de.iip_ecosphere.platform.support.json.JsonUtils;
import iip.datatypes.*;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.fasterxml.jackson.core.JsonProcessingException;

/**
 * JSON transport serializer for MyNested.
 * Generated by: EASy-Producer.
 */
public class MyNestedSerializer implements Serializer<MyNested> {

    private static final ObjectMapper MAPPER = new ObjectMapper();
    static {
    }

    @Override             
    public MyNested from(byte[] data) throws IOException {
        try {
            return MAPPER.readValue(data, MyNested.class);
        } catch (JsonProcessingException e) {
            throw new IOException(e);
        }
    }

    @Override    
    public byte[] to(MyNested source) throws IOException {
        try {
            return MAPPER.writeValueAsBytes(source);
        } catch (JsonProcessingException e) {
            throw new IOException(e);
        }
    }

    @Override
    public MyNested clone(MyNested origin) throws IOException {
        return new MyNested(origin);
    }

    @Override
    public Class<MyNested> getType() {
        return MyNested.class;
    }

}

