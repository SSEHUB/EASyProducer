package ReceiverImpl;

import java.util.concurrent.ExecutionException;

import de.iip_ecosphere.platform.services.environment.ServiceState;

import iip.datatypes.RestPhoenixEEMMixed;
import iip.datatypes.RestPhoenixEEMMixedImpl;

import org.junit.After;
import org.junit.Before;
import org.junit.Test;
import org.slf4j.LoggerFactory;

/**
 * IIP-Ecosphere service jUnit test for 'REST Receiver'.
 * Generated by: EASy-Producer.
 */
public class ReceiverImplTest {

    private ReceiverImpl service;

    /**
     * Tests processing a data item of type "RestPhoenixEEMMixed" asynchronously.
     */
    @Test
    public void testProcessRestPhoenixEEMMixed() {
        RestPhoenixEEMMixed data = new RestPhoenixEEMMixedImpl();
        // TODO fill data via setters
        service.processRestPhoenixEEMMixed(data);
        // no direct output for a sink. may be it's generating a log, a file, etc. to assert
    }

    /**
     * Initializes the test.
     */
    @Before
    public void startup() {
        // constructor for testing, does not set up service management values!
        service = new ReceiverImpl();
        try {
            service.setState(ServiceState.STARTING);
        } catch (ExecutionException e) {
            LoggerFactory.getLogger(getClass())
                .error("Cannot start service: {}", e);
        }
    }

    /**
     * Shuts down the test.
     */
    @After
    public void shutdown() {
        try {
            service.setState(ServiceState.STOPPING);
        } catch (ExecutionException e) {
            LoggerFactory.getLogger(getClass())
                .error("Cannot stop service: {}", e);
        }
    }

}
