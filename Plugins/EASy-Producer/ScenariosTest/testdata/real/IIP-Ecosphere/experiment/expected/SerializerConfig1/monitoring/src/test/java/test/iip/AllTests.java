package test.iip;

import java.io.File;
import java.io.FileInputStream;
import java.io.IOException;

import org.junit.Test;

import static de.iip_ecosphere.platform.support.setup.AbstractSetup.readFromYaml;

/**
 * Tests a Yaml configuration file. Must be done during build as then all dependencies are determined and available.
 * Generated by: EASy-Producer.
 */
public class AllTests {

    /**
    * Tests reading a Yaml configuration file.
    *
    * @throws IOException shall not occur if test is successful
    */
    @Test
    public void testCfg() throws IOException {
        try (FileInputStream fis = new FileInputStream(new File("src/main/resources/iipecosphere.yml"))) {
            readFromYaml(de.iip_ecosphere.platform.monitoring.MonitoringSetup.class, fis);
        } catch (IOException e) {
            // close file and re-throw
            throw e;
        }
    }

}
