package iip.serializers;

import java.io.IOException;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.databind.ObjectMapper;

import de.iip_ecosphere.platform.support.json.JsonUtils;
import de.iip_ecosphere.platform.transport.serialization.Serializer;

import iip.datatypes.*;

/**
 * JSON transport serializer for SentronOutput.
 * Generated by: EASy-Producer.
 */
public class SentronOutputSerializer implements Serializer<SentronOutput> {

    private static final ObjectMapper MAPPER = new ObjectMapper();

    static {
        JsonUtils.handleIipDataClasses(MAPPER);
    }

    @Override
    public SentronOutput from(byte[] data) throws IOException {
        try {
            return MAPPER.readValue(data, SentronOutputImpl.class);
        } catch (JsonProcessingException e) {
            throw new IOException(e);
        }
    }

    @Override
    public byte[] to(SentronOutput source) throws IOException {
        try {
            return MAPPER.writeValueAsBytes(source);
        } catch (JsonProcessingException e) {
            throw new IOException(e);
        }
    }

    @Override
    public SentronOutput clone(SentronOutput origin) throws IOException {
        return new SentronOutputImpl(origin);
    }

    @Override
    public Class<SentronOutput> getType() {
        return SentronOutput.class;
    }

}
