Command processor demo product line: EASyDemoCommandsAspects

Instantiates variabilities using aspects. As the auto build mechanism of Eclipse recompiles the
main class and removes the woven aspects, we compile the project into the directory "build" and
provide a relocatable launch config.

Main class: io.ssehub.easy.demo.command.constants.Main